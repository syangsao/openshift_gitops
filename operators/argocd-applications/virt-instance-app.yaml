apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: virt-instance
  namespace: openshift-gitops
spec:
  project: default
  source:
    repoURL: 'https://github.com/syangsao/openshift_gitops.git'
    targetRevision: main
    path: operators/virt-instance
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: openshift-virtualization-operator
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
